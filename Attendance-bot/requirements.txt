# ============================================================
# Attendance Bot — Requirements
# Install via setup.bat (NOT pip install -r directly)
# setup.bat handles order and special cases for Windows
# ============================================================

# Browser automation
playwright==1.44.0

# Scheduling
schedule==1.2.1
python-dateutil==2.9.0
pytz==2024.1

# Speech recognition
SpeechRecognition==3.10.4
# pyaudio — installed via pipwin in setup.bat (Windows binary)
sounddevice==0.4.6
soundfile==0.12.1
numpy==1.26.4

# Whisper (GPU speech fallback) — needs setuptools>=65 first
# openai-whisper==20231117  ← installed after setuptools upgrade

# HTTP / WebSocket
requests==2.32.2
websockets==12.0
aiohttp==3.9.5

# Dashboard
flask==3.0.3
flask-socketio==5.3.6

# Excel output
openpyxl==3.1.2

# Utilities
python-dotenv==1.0.1
colorlog==6.8.2
rich==13.7.1
psutil==5.9.8
Pillow==10.3.0

# PyTorch + CUDA — installed separately in setup.bat via:
# pip install torch --index-url https://download.pytorch.org/whl/cu121
